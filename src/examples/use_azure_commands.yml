description: >
  Example of job using the command `blt-deploy`
usage:
  version: 2.1
  orbs:
    az: mobomo/az-orb@1.0.0
  jobs:
    deploy:
      executor:
        az/default
      steps:
        - checkout
        - az/blt-deploy
